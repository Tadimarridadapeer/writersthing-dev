import * as React from'react';
import { Section, Text, Row, Column, Img, Button, Container } from'@react-email/components'; export interface BookCardProps { coverUrl: string; title: string; author: string; description: string; ctaText: string; ctaUrl: string;
} export const BookCard = ({ coverUrl, title, author, description, ctaText, ctaUrl }: BookCardProps) => { return ( <Section className="bg-white border border-gray-200 rounded-lg p-[16px] mb-[16px]"> <Row> <Column className="w-[100px] pr-[16px]" style={{ verticalAlign:'top' }}> <Container className="bg-gray-100 rounded overflow-hidden"> <Img src={coverUrl} width="100" height="150" alt={title} className="object-cover" /> </Container> </Column> <Column style={{ verticalAlign:'top' }}> <Text className="text-black font-bold text-[18px] m-0 leading-[24px]">{title}</Text> <Text className="text-gray-500 font-medium text-[14px] mt-[4px] mb-[8px]">by {author}</Text> <Text className="text-gray-700 text-[14px] leading-[22px] m-0 mb-[16px]">{description}</Text> <Button href={ctaUrl} className="bg-black text-white text-[14px] font-semibold rounded py-[8px] px-[16px] text-center w-auto no-underline" > {ctaText} </Button> </Column> </Row> </Section> );
}; export default BookCard;
