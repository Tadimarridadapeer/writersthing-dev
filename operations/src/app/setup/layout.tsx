import React from "react";

export const metadata = {
  title: "Setup — Operations Portal",
  description: "Create your Super Admin account",
};

export default function SetupLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
